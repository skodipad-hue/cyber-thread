<!DOCTYPE html>
<html>
<head>
    <title>Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-black text-gray-200 min-h-screen flex justify-center items-center font-mono">

<div class="bg-[#12121a] p-8 rounded-xl max-w-xl w-full shadow-lg border border-cyan-400/30">

    <h2 class="text-cyan-400 text-xl font-bold">@<%= post.username %></h2>
    <p class="text-xs text-gray-400 mb-4"><%= post.timeAgo %></p>

    <p class="text-gray-200 whitespace-pre-wrap mb-4"><%= post.content %></p>

    <% if (post.url) { %>
        <img src="<%= post.url %>" class="rounded-lg border border-cyan-400/30 shadow-lg mb-4">
    <% } %>

    <a href="/users/<%= post.user_id %>/posts"
        class="text-cyan-400 underline">
        ← Back to Feed
    </a>
</div>

</body>
</html>
