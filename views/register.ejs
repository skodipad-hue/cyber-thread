<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join In</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-black via-gray-900 to-black text-green-400">

    <div class="w-full max-w-md bg-black/70 border border-green-500 rounded-xl p-8 shadow-[0_0_25px_#22c55e]">

        <h1 class="text-3xl font-bold text-center mb-6 tracking-widest">
            JOIN IN
        </h1>

        <!-- REGISTER FORM -->
        <form method="POST" action="/register" class="space-y-5">

            <div>
                <label class="block mb-1 text-sm tracking-wide">Username</label>
                <input 
                    type="text" 
                    name="username" 
                    required
                    class="w-full bg-black border border-green-500 rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400"
                >
            </div>

            <div>
                <label class="block mb-1 text-sm tracking-wide">Email</label>
                <input 
                    type="email" 
                    name="email" 
                    required
                    class="w-full bg-black border border-green-500 rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400"
                >
            </div>

            <div>
                <label class="block mb-1 text-sm tracking-wide">Password</label>
                <input 
                    type="password" 
                    name="password" 
                    required
                    class="w-full bg-black border border-green-500 rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-green-400"
                >
            </div>

            <button 
                type="submit"
                class="w-full mt-4 py-2 rounded-md font-semibold tracking-widest
                       bg-green-500 text-black hover:bg-green-400
                       transition-all duration-300 shadow-[0_0_15px_#22c55e]"
            >
                CREATE ACCOUNT
            </button>

        </form>

        <!-- BACK TO LOGIN -->
        <form method="GET" action="/login" class="mt-6 text-center">
            <button 
                class="text-sm text-green-400 hover:text-green-300 underline tracking-wide"
            >
                Back to Login
            </button>
        </form>

    </div>

</body>
</html>
