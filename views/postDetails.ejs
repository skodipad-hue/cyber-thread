<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Post Details</title>

  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#0a0a0f] text-gray-200 font-mono min-h-screen">

<a href="/users/<%= post.user_id %>/posts"
   class="m-6 inline-block px-4 py-2 bg-pink-500 text-white rounded-lg font-bold">
   ← Back to Feed
</a>

<div class="max-w-5xl mx-auto bg-[#12121a] border border-cyan-400/20 rounded-xl p-10">

  <div class="flex gap-10">

    <!-- POST IMAGE -->
    <div class="w-1/2 flex items-center justify-center bg-black rounded-xl">
      <% if (post.url) { %>
        <img src="<%= post.url %>" class="rounded-lg max-h-72">
      <% } else { %>
        <p class="text-gray-500">No Photo Uploaded</p>
      <% } %>
    </div>

    <!-- USER INFO -->
    <div class="w-1/2">

      <!-- PROFILE IMAGE -->
      <div class="flex items-center gap-4 mb-4">
        <div class="w-16 h-16 rounded-full border border-cyan-400/30 overflow-hidden bg-black flex items-center justify-center">
          <% if (post.profile_url) { %>
            <img src="<%= post.profile_url %>" class="w-full h-full object-cover">
          <% } else { %>
            <span class="text-gray-500 text-xs">No Image</span>
          <% } %>
        </div>

        <div>
          <h1 class="text-2xl text-cyan-400 font-bold">@<%= post.username %></h1>
          <p class="text-gray-400 text-sm"><%= post.email %></p>
        </div>
      </div>

      <p class="text-sm text-gray-400">
        <%= post.timeAgo %> · <%= new Date(post.created_at).toLocaleString() %>
      </p>

    </div>
  </div>

  <div class="mt-8">
    <h2 class="text-xl text-cyan-400 mb-2">Post Content</h2>
    <p class="bg-black p-4 rounded-xl whitespace-pre-wrap">
      <%= post.content %>
    </p>
  </div>

</div>

</body>
</html>
