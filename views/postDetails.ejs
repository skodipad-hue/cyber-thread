<!DOCTYPE html>
<html>
<head>
  <title>Post Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#0a0a0f] text-gray-200 font-mono min-h-screen">

<a href="/users/<%= post.user_id %>/posts"
   class="m-6 inline-block px-4 py-2 bg-pink-500 text-white rounded shadow">
  ← Back to Feed
</a>

<div class="max-w-5xl mx-auto bg-[#12121a] p-10 rounded-xl border border-cyan-400/20 shadow-xl">

  <div class="flex items-center gap-6 mb-6">

    <!-- PROFILE IMAGE -->
    <div class="w-20 h-20 rounded-full overflow-hidden border border-cyan-400/30">
      <% if (post.profile_url) { %>
        <img src="<%= post.profile_url %>" class="w-full h-full object-cover">
      <% } else { %>
        <div class="w-full h-full flex items-center justify-center text-gray-500 text-xs">
          No Image
        </div>
      <% } %>
    </div>

    <div>
      <h1 class="text-2xl text-cyan-400 font-bold">@<%= post.username %></h1>
      <p class="text-gray-400"><%= post.email %></p>
      <p class="text-sm text-gray-500"><%= post.timeAgo %></p>
    </div>
  </div>

  <!-- POST IMAGE -->
  <% if (post.url) { %>
    <img src="<%= post.url %>"
         class="rounded-xl mb-6 max-h-[400px] mx-auto shadow-lg">
  <% } %>

  <p class="bg-black/40 p-6 rounded border border-cyan-400/10 whitespace-pre-wrap">
    <%= post.content %>
  </p>

</div>

</body>
</html>
